import torch
tensor = torch.tensor([1,2,3])

tensor = tensor + 10
print(tensor)
# built-in functions
tensor = torch.multiply(tensor, 10)
print(tensor)

tensor = tensor * 10
print(tensor)


tensor = tensor - 10
print(tensor)
